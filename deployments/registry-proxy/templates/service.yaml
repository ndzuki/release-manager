apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: registry-proxy
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 5000
    nodePort: {{ .Values.service.nodePort }}
  selector:
    app: registry-proxy
