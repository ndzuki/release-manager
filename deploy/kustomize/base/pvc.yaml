apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: release-manager-orchestrator-data
  namespace: release-manager-dev
  labels:
    app.kubernetes.io/part-of: release-manager
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
