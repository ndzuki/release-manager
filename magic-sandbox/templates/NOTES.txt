Magic Sandbox 部署成功！

部署时间: {{ now | date "2006-01-02 15:04:05" }}
Chart 版本: {{ .Chart.Version }}
应用版本: {{ .Chart.AppVersion }}
命名空间: {{ .Release.Namespace }}

访问信息:
- Magic Gateway: http://magic-gateway.{{ .Release.Namespace }}.svc.cluster.local
- Sandbox Gateway: http://sandbox-gateway.{{ .Release.Namespace }}.svc.cluster.local
{{- if index .Values "sandbox-gateway" "ingress" "enabled" }}
- 外部访问: http://{{ (index (index .Values "sandbox-gateway" "ingress" "hosts") 0).host }}
{{- end }}

注意事项:
1. 请设置正确的 API Keys
2. 请检查 Ingress 配置是否符合您的域名要求
3. 请确保 S3 存储配置正确

查看 Pod 状态:
  kubectl get pods -n {{ .Release.Namespace }}

查看服务状态:
  kubectl get svc -n {{ .Release.Namespace }}

