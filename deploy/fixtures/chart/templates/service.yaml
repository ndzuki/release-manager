apiVersion: v1
kind: Service
metadata:
  name: release-fixture
  labels:
    app.kubernetes.io/name: release-fixture
    app.kubernetes.io/part-of: release-manager
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: release-fixture
  ports:
    - name: http
      port: 8088
      targetPort: http
